# Copy this file to .env.local for local overrides.
# In development, the proxy in package.json handles API routing automatically.

# Optional: set this if deploying frontend separately from backend
# REACT_APP_API_URL=http://localhost:8000
